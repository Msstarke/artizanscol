{
  "name": "artizans-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build --silent && node --test dist/tests/**/*.test.js",
    "package:lambda": "./scripts/build-lambda-package.sh"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.914.0",
    "@aws-sdk/client-secrets-manager": "^3.914.0",
    "@aws-sdk/lib-dynamodb": "^3.914.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.13.11",
    "typescript": "^5.8.2"
  }
}
